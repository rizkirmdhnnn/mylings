services:
  mylings:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    ports:
      - "8088:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    restart: unless-stopped

networks:
  default:
    name: mylings-network
